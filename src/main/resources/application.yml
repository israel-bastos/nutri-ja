spring:
  datasource:
#    url: jdbc:mysql://localhost:3306/nutri-ja_database?createDatabaseIfNotExist=true =${DATABASE_URL}
#    username: root =${USER}
#    password: root =${PASSWORD}

      url: jdbc:mysql:${DATABASE_URL}
      username: ${USER}
      password: ${PASSWORD}


  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  thymeleaf:
    cache: false

server:
  error:
    whitelabel:
      enabled: false
